<UserControl x:Class="IntegratedJogPanel.View.RealNumberEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IntegratedJogPanel.View"
             mc:Ignorable="d" 
             Height="30" Width="128">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="26"/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" x:Name="_txtVal" HorizontalContentAlignment="Right" VerticalContentAlignment="Center" FontSize="18" MaxLength="7"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <RepeatButton Grid.Row="0" x:Name="_btnUp">
                <Path Fill="Gray" Data="M5,0 L0,5 10,5 Z"/>
            </RepeatButton>
            <RepeatButton Grid.Row="1" x:Name="_btnDown">
                <Path Fill="Gray" Data="M5,5 L0,0 10,0 Z"/>
            </RepeatButton>
        </Grid>
        <TextBlock Grid.Column="2" x:Name="_txtUnitLabel" Text="mm" VerticalAlignment="Bottom" Margin="4 0 0 0"/>
    </Grid>
</UserControl>
