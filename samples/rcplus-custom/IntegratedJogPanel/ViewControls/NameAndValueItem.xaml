<UserControl x:Class="IntegratedJogPanel.View.NameAndValueItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IntegratedJogPanel.View"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" x:Name="_borderHead" BorderBrush="LightGray" BorderThickness="0 1 0 0" Visibility="Collapsed"/>
        <Border Grid.Row="1" Grid.Column="0" BorderBrush="LightGray" BorderThickness="1 0 1 1" Background="#E0E0E0">
            <TextBlock x:Name="_txtBlockName" Margin="4 0" HorizontalAlignment="Right" FontSize="11"/>
        </Border>
        <Border Grid.Row="1" Grid.Column="1" BorderBrush="LightGray" BorderThickness="0 0 1 1" Background="#FFFFFF">
            <TextBlock x:Name="_txtBlockValue" Margin="4 0" HorizontalAlignment="Right" FontSize="11"/>
        </Border>
    </Grid>
</UserControl>
