<Window x:Class="IntegratedJogPanel.SelectRCPlusPtsFileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IntegratedJogPanel"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Title="{Binding Path=[LoadPtsWin_Title].Value, Source={x:Static local:Main.Captions}}" Height="200" Width="300">
    <DockPanel Margin="8">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="_btnOK" Content="{Binding Path=[Common_OK].Value, Source={x:Static local:Main.Captions}}" Margin="0 0 4 0"/>
            <Button x:Name="_btnCancel" Content="{Binding Path=[Common_Cancel].Value, Source={x:Static local:Main.Captions}}"/>
        </StackPanel>
        <ListBox x:Name="_listBox" Margin="0 0 0 8"/>
    </DockPanel>
</Window>
